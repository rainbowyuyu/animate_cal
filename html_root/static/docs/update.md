
---

## 初始版
- 完成基本的从streamlit页面到h5页面的静态内容的迁移和基础样式的搭建

## v 0.1.1
- 正确完成了检测功能

## v 0.1.2
- 修复
  - 完成了logo的设计并接入logo
  - 修复了图片预览不能正常显示的问题
  - 手写功能不能正常使用了
  - 切换上传时状态栏不改变
  - 不能正确检测上传的图片

## v 0.1.3
- 新增
  - 在设置中增加版本管理，能够通过update.md文件的版本号启用
- 优化
  - 页脚的样式和内容
  - logo变为透明

## v 0.1.4
- 优化
  - 优化样式，整体是白色的网页，上面的内容和页脚做一个适配，保持整体的配色

## v 0.2.1
- 新增
  - mysql数据库，将登录等数据存储在mysql数据库中
  - 新增了一个页面，可以存储用户的算式，方便在计算页面直接使用，并保存在数据库中
  
## v 0.2.2
- 优化
  - 算式库中的算式可以编辑和重命名，并且预览时有规范功能，比如规范去掉$$等符号
  - 算式库添加一个加号，可以到智能识别页面识别或添加算式

## v 0.2.3
- 新增
  - 计算页面可以从公式库导入公式，也可以像公式库那样进行在线编辑
- 优化
  - 计算和导入中的算式可以编辑和重命名，并且预览时有规范功能，比如规范去掉$$等符号

## v 0.2.4
- 新增
  - 接入生成动画逻辑，用户输入算式后，将算式交给大模型，大模型对算式进行正确的组合，然后要求大模型生成正确的manim代码，优化提示词，保证manim正确不出现MathTex的latex不正确等问题，用户可以看见生成动画代码的过程，生成完代码后，写入一个临时的py文件，通过调用manim -ql指令进行渲染，渲染进程完后读取渲染的内容进行展示给用户，渲染过程中给用户进度条
  - 生成过程中的所有内容流式反馈给用户
- 优化
  - 优化帮助页面的交互反馈内容

## v 0.2.5
- 优化
  - 优化生成manim代码的提示词和意图识别
  - 优化生成python代码的流式生成，为渲染提供充足的时间
  - 优化了教程组件
  - 实装教学案例
- 新增
  - 新增完善隐私政策，api文档，服务协议等文档，将对应链接实装

## v 0.2.6
- 整理
  - 整理文档信息和相关安装要求，转变为正式上线版本

## v 0.2.7
#### 内测版 Ⅰ
- 新增
  - 上传图像添加剪贴板功能
  - 作者rainbow鱼的简介链接
- 优化
  - 规范提示词，约束latex中不能出现中文且Transform函数仅限于copy形式
  - 手机版的各个子网页样式

## v 0.2.8
#### 内测版 Ⅱ
- 优化
  - 优化用户注册和登录的验证码功能，优化图像生成的逻辑，并且不会第一次输入时一直验证码错误
  - 整理计算页面的样式css文件
  - 教学案例的视频从悬浮鼠标播放优化点击后能进入一个大页面正式视频播放
- 修复
  - 去计算按钮正确将公式复制到计算计算页面
- 修改
  - 去掉公式栏b，现在只能在一个公式栏中填写和编辑公式，并能显示latex代码
  - 适当调整计算界面的样式，更加贴合

## v 0.2.9
#### 内测版 Ⅲ
- 修改
  - 修改了库的项目名为《智算视界》统一，并修改了对应的GitHub链接
- 新增
  - 解题中适当添加画图的功能进一步动态可视化
  - 设置中新增了深色模式和浅色模式调整的按钮
  - 新增了深色模式下深色的配色方案和各个组件的适配
- 优化
  - 优化教程，改成具体的每一步应该做什么，便于第一次使用的理解

## v 0.3.1
#### 公测版 Ⅰ
`2026.01.28`
- 修复
  - 修复了教程步骤不适配深色模式的问题
  - 由于计算页面公示栏组件名称问题导致的去计算链接失效问题
- 优化
  - 优化验证码在服务器上会看不清的问题
  - 计算工作界面用户可以看到本次生成的代码而不是在渲染完成视频后就直接替换掉历史代码部分
  - 选择模式区分了推演和可视化部分，保证代码的稳定性
  - 优化生成逻辑，避免不存在的参数存在于生成中导致报错产生以及latex的规则完善
- 新增
  - 专项部分，写入专项的加强可视化代码，为专项的可视化演示提供精细的调整

## v 0.3.2
#### 公测版 Ⅱ
`2026.02.01`
- 新增
  - 注册时需要输入两边密码验证，并且注册和登录需要勾选隐私协议
  - 主页面板的动画选项卡点击后会进入对应的页面
  - 添加开发者工具模块，可以进行latex编辑和manim渲染
  - 底部页脚资源链中添加开发者模块的链接
- 优化
  - 登录后有1天时间会记录登录状态，不需要重复登录除非手动登出
  - MathLive公式输入面板在转换页面时也会自动关闭
  - `保存并查看` 和 `去计算` 按钮需要先识别成功后才会被激活，没有识别时处于冷却状态
  - `生成可视化动画` 按钮在生成视频后会处于30s冷却状态，防止持续点击
  - 资源链支持4行排布，并且在手机上以标题居中，2列分布的形式优化样式
- 修改
  - 修改帮助页面文字的准确性

## v 0.3.3
#### 公测版 Ⅲ
`2026.02.04`
- 优化
  - 采用 `Monaco` 替换原来的代码编辑区域，类 vscode 内核，并且接入 pylance 升级代码的用户体验
- 修复
  - 修复由于 Monaco 和 MathLive 的 value 带来的函数冲突
- 新增
  - 接入 `rainbow_extend_manim` 的 manim 拓展库，用户可以免安装直接使用拓展库的案例
  - 添加url解析，可以通过url直接访问对应的模块
  - 单独创建一个文件方便管理，优化devtools有图文并茂的感觉像是一个模块便于用户交互

## v 0.3.4
#### 外观样式版本更新
`2026.02.13`
- 修复
  - 在深色模式下，使用笔和橡皮手写算式时颜色错误的问题
  - 登录后无法正常登出
  - 输入验证码时第一次验证码总是不正确的问题
- 调整
  - timeout调整为合理的时间，从180s改为300s兼容更多更复杂的算式可视化
  - 对MathLive的LaTeX界面的整体视觉美观进行调整
- 优化
  - 优化timeout的报错提示，让用户知道错误是由于服务器性能和算式复杂度导致的
  - 优化MathLive面板的样式以及交互体验
  - 优化设置界面快捷键更改，在其他位置点击后能直接定位到更改按键的位置
  - 解决了手机端中画板触摸事件的逻辑，防止在上下滑动时误触，根据第一段移动方向再决定是 `滚动`还是`画画`
  - 优化了手机端识别公式后由于公式太长会遮挡的问题
  - 手机端一些案件会由于文字过多纵向排列
  - 将MathLive界面的内容变为中文
  - 开发者工具中的最终渲染预览的大小
  - 打开更新日志按钮展示markdown时，自动跳转到最新的版本
  - 优化系统设置页面滚轮条的样式，嵌入圆角面板，保证title和关闭浮动于顶部
  - 优化登录后反馈的提示框样式
- 新增
  - 新增画板界面各个操作的快捷键
  - 添加用户名查询机制防止出现重复的用户名

## v 0.3.5
#### 智能体和渲染版本更新
`2026.02.16`
- 新增
  - **智能体**：在首页后新增智能体页面，用户用自然语言描述需求（可上传公式图片），网站自动理解意图并**跳转到对应步骤**完成任务（如跳转智能识别、动态计算、开发者工具等，并预填公式或自动执行生成）。顶部导航、首页、页脚、帮助页均已接入智能体入口与说明。
  - 原`一键渲染`改为`理解意图 → 调用本站工具`：智能体不再在后端直接渲染，而是调度识别、计算、开发者工具等现有功能。
  - 我的算式界面添加一个子页面，用户渲染出想要的内容后，可以保存渲染的代码，代码部分同样使用Monaco引擎也能进行后期的编辑等操作，可以直接去开发者工具中进行编辑和渲染
  - 当manim渲染报错时，自动发送错误信息给大模型进行改正和重新生成
  - 云端渲染工作台中添加导入算式的界面，用户可以从保存的脚本和manim_extend_rainbow库中的样例选择
- 优化
  - 优化manim生成动画的渲染速度
  - manim渲染时保留渲染的日志进度，给告知用户渲染的实时情况
  - 云端渲染工作台可以拖动交界处调整大小
  - 优化一些兼容性问题

## v 0.3.6
#### 智能体能力和设置版本更新
`2026.02.20`
- 新增
  - 新增智能体能力，用户可以说多个事件，智能体会根据要求一步步执行
  - 智能体可以解决用户说的比如打开云端渲染工作台并且帮我写一段manim示例代码填入，帮我识别公式并且保存到我的算式等等复杂功能
  - 现在通用计算可视化会讲计算和可视化分步执行，并分两个视频展现给用户，而不是以前在一整段代码中，并添加标签，用户存储代码时知道是计算还是可视化，增加分步计算的容错
  - 新增丰富具体化的设置，自动保存不同用户的设置，并在下次登录时加载
  - 新增用户设置，用户可以创建或修改自己的信息
- 优化
  - 优化智能体界面的布局，并且添加一个按钮，可以告诉用户全部的功能和示例，用户点击后可以试着执行
  - 画板中鼠标不在只是准星，而是用小圆圈根据画笔大小显示这一笔的圆圈，并且在画板中使用alt+右键+鼠标滑动可以试试预览显示地改变大小，左滑是变小优化是变大，类似于Photoshop的效果
  - 智能体的头像改为智算视界的logo，所有交互的输入名称等的提示框改为网站样式
  - 所有的保存和提示窗体都变为网站样式
  - 优化教程内容，将新的内容更新上
- 修复
  - 修复画板功能第一笔不能撤回问题，以及按画笔或橡皮的快捷键时无论按那个都会切换的问题
  - 修复修改快捷键后图标后小括号的快捷键显示错误
  - 智能体识别图片时如果用户没有图片则会提示用户上传图片，不先调用工具
- 整理
  - 整理api中py文件的文件结构

## v 0.3.7
#### 智能体解析和计算并行更新
`2026.02.20`
- 新增
  - 智能体解析用户给的图片和算式时，能先将题目解析为 LaTeX 可编辑算式或 Manim 可渲染算式，支持拆解、解析题目
  - 动态计算在`计算`和`可视化`前新增结果窗口，先返回文字版题目计算结果，再进行动态计算或可视化
  - 智能体和画板页面在上传图片后再次点击图片可以进行类似微信的编辑，比如裁剪等等操作，鼠标浮动到图片预览上后显示点击编辑，然后进入编辑界面
- 优化
  - 计算若出现错误会尝试纠正最多 3 次，并总结错误原因、提示用户可再次生成
  - 智能体执行可视化时，进入动态计算页后自动触发`生成可视化动画`按钮
  - 智能体页面功能与展示增加醒目标识，便于一眼看到；示例改为单独扩展窗体供选择，并补充组合实用示例
  - 项目整体样式优化
- 修复
  - 修复画板的锁定功能，锁定后不能画图案，只能滑动，解锁后点击屏幕可以画图，取消之前的下滑判定，修复下滑时会画不出来的问题
  - 修复一些js的报错bug
  - 修复一些窗口的层次显示问题和加载逻辑

## v 0.3.8
#### 解题演示和视频播放系统更新
`2026.02.22`
- 新增
  - 设置页`默认演示模式`增加`完整解题演示`选项，与计算页演示模式下拉一致，选择后计算页状态栏同步
  - 智能体流式回复支持 Markdown 解析，支持加粗、列表等排版
  - 教学案例的视频采用鉴权功能，增加加载速度，新增结合WebSocket的点赞弹幕功能，增加用户的观感互动，能够记录用户看过哪些片段时间点，显示于贝塞尔波形
- 优化
  - 全站 LaTeX 改用 KaTeX 渲染，公式整齐正确好看，支持 `$...$` 与 `$$...$$`
  - 数学表达式字号移至设置页动态计算项，用滑动条调节并显示数值，默认小字号便于看全长题，计算页不再展示大中小按钮
  - 完整解题演示生成视频分阶段分屏展示，选项数与图数一致（如四选项画四幅图），文字公式不重合，避免一屏挤满
- 修复
  - 计算页选择演示模式时同步到设置，设置页选择默认演示模式时同步到计算页状态栏
  - 完整解题 Manim 提示词强化分阶段展示与文字不重合约束

## v 0.3.9
#### 收藏、稍后看、标签筛选与全站搜索
`2026.02.24`
- 新增
  - 更新日志解析增强：打开更新日志后顶部生成「跳转到版本」按钮栏（自 v 0.x.x 起），点击即跳转并高亮对应版本；文档内可书写 [按钮文字](#update-v-0.3.8) 形式的锚点链接，点击后跳转并高亮该节 [定位](#update-v-0.3.9)
  - 教学案例支持收藏与稍后看，登录后可在卡片和播放页进行收藏/加入稍后看，案例页提供「全部 / 收藏 / 稍后看」筛选
    > 体验入口： [去定位筛选栏](#section-examples-filter)
  - 教学案例支持按标签筛选，每个案例展示标签，metadata 中可配置 tags
    > 体验入口： [去定位筛选栏](#section-examples-filter)
  - 教学案例播放页支持时间戳笔记，登录后可在当前时间点添加笔记，笔记列表点击可跳转至对应时间
    > 体验入口： [去教学案例](#section-examples)
  - 教学案例播放页支持复制直达链接（含当前时间点），访问链接可打开对应视频并定位到时间点
    > 体验入口： [去教学案例](#section-examples)
  - 导航栏新增全站搜索，可搜索我的算式、动画脚本与教学案例，结果分类型展示并支持一键跳转
    > 体验入口： [去首页使用搜索](#section-home)
  - 角色化使用引导：首页新增「按角色快速开始」区块，支持学生 / 教师 / 创作者 / 开发者四类入口，一键唤起智能体并预填推荐提示词，引导进入适合该角色的识别 / 计算 / 案例 / 开发者工具组合路径
    > 体验入口： [去首页角色入口](#section-home-roles)
  - 教学案例学习闭环与练习绑定：教学案例播放页新增「复习推荐」提示上次观看到的位置，并基于心跳记录给出续看起点；时间戳笔记支持一键转成练习题，唤起智能体生成同类题（含步骤答案），题目自动带回链到原视频时间点
    > 体验入口： [去教学案例](#section-examples)
  - 可复用动画组件：Rainbow Manim 扩展区升级为组件卡片视图，每个模块展示说明与代码示例，支持在开发者工具页一键载入云端工作台试用，并可通过导入面板从模块列表写入 Monaco 编辑器
    > 体验入口： [去开发者工具](#section-devtools)
  - 教师课件入口：教学案例页新增「我的课件」分组与「创建课包」按钮，提供一键课件流水线的入口与说明弹窗，为后续从 PPT / 板书自动生成课件包做准备
    > 体验入口： [去教学案例](#section-examples)
  - 创作者发布工具：教学案例播放页新增「导出发布包」按钮，可导出包含弹幕与元信息的 JSON；Manim 工作台增加「生成 B 站文案」按钮，根据脚本自动生成标题与简介草稿
    > 体验入口： [去开发者工具](#section-devtools)
  - Rainbow 社区模块占位：拓展库面板新增「社区模块」说明区，预告未来支持上架社区 Manim 模块，在站内一键载入与 fork 改编
    > 体验入口： [去开发者工具](#section-devtools)
  - 智能体编排模板：智能体回复中展示本次自动执行的步骤树（如识别 → 转 LaTeX → 生成脚本 → 渲染），支持一键「存为模板」并在侧边栏「从模板运行」中复用；图片上传支持多选，并自动在提示词中标注多图信息
    > 体验入口： [去智能体](#section-agent)
- 优化
  - 案例列表接口支持 `tag`、`filter_mode` 参数，便于前端筛选与「我的收藏/稍后看」展示
    > 体验入口： [去定位筛选栏](#section-examples-filter)
  - URL 支持 `?section=examples&video=xxx&t=秒数` 直达教学案例并定位播放
    > 体验入口： [去教学案例](#section-examples)

## v 0.4.0
#### 等待体验、工具流动性与移动端策略优化
`2026.02.26`
- 新增
  - 动态计算页渲染阶段状态栏：在生成动画过程中，顶部「正在生成与渲染」状态文案会随步骤自动切换（理解题目 → 生成脚本 → 自动纠错 → 渲染视频），结合右侧日志与代码预览，让用户清楚知道系统当前在做什么、做到哪一步了
    > 体验入口： [去动态计算](#section-calculate)
  - 解题步骤独立流式窗口增强：题解以 Markdown 形式实时流式写入「解题步骤」窗口，滚动自动跟随最新内容，支持公式高亮与列表/标题排版，既可配合智能体，也可单独在动态计算页查看
    > 体验入口： [去动态计算](#section-calculate)
  - 关键帧预览占位：渲染阶段支持从后端接收可选的 `preview_url` 字段，前端会在视频区优先展示一张关键帧预览图，再逐步切换为完整 MP4，降低长时间黑屏等待的焦虑感（后端按需接入即可）
    > 体验入口： [去动态计算](#section-calculate)
  - 视觉描述 Prompt 传递管道：智能识别成功后，除 LaTeX 公式外，还会将后端返回的视觉描述 Prompt 暂存到会话中；在动态计算调用动画生成接口时一并附带，便于后端针对「这是一个圆/矩阵/几何关系」等信息做更贴合含义的可视化
    > 体验入口： [去智能识别](#section-detect) / [去动态计算](#section-calculate)
- 优化
  - 移动端「采集器 + 播放器」策略：在手机端不再初始化云端开发者工具工作台，并隐藏导航中的「开发者工具」入口；如在移动端通过链接或智能体跳转到 `devtools`，会提示需在电脑/平板端打开，手机端重点用于拍照识别、查看题解和回看视频
    > 体验入口： [去首页角色入口](#section-home-roles) / [去智能体](#section-agent)
  - 等待体验文案细化：渲染过程中的多处 Loading 文案与占位提示统一改为「阶段式」描述（例如「正在理解题目与规划步骤…」「正在生成动画脚本与关键步骤…」「检测到错误，正在自动修正脚本…」「正在渲染视频画面…」），避免用户误以为页面卡死
    > 体验入口： [去动态计算](#section-calculate)
  - detect → calculate 上下文连续性：从智能识别跳转到动态计算时，公式仍通过 MathLive 主输入框同步，额外的视觉 Prompt 通过前端管道传递给动画接口，不改变现有使用习惯的同时，为后端继续升级「从图到动画」留下空间
    > 体验入口： [去智能识别](#section-detect)
  - 移动端画板锁定按钮逻辑复用统一的设备检测方法，在不同尺寸设备间切换时锁定/解锁状态更加稳定，滑动和书写的误触几率进一步降低
    > 体验入口： [去智能识别](#section-detect)
- 安全
  - 文档与智能体 Markdown 渲染安全加固：对 `update.md` 等文档、智能体回复以及开发者工具中的 Markdown 内容，在渲染为 HTML 后统一移除 `<script>` 标签、内联 `on*` 事件与 `javascript:` 链接，降低 XSS 风险
    > 体验入口： [去帮助文档](#section-help) / [去智能体](#section-agent) / [去开发者工具](#section-devtools)
  - 保持 KaTeX 渲染配置通过统一的 `renderMath(container)` 调用集中管理，同时避免直接在 Markdown 中执行任意 HTML，兼顾公式显示效果与前端安全性

## v 0.4.1
#### 右键菜单区域化、子页面导航与交互体验优化
`2026.02.27`
- 新增
  - 全站知识图谱：首页「按角色快速开始」支持进入全站知识图谱，以 3D 力导向图展示功能节点（智能体、智能识别、动态计算、开发者工具、教学案例等）及其子工具，点击节点可跳转至对应页面；智算星云地铁式导航基于图谱数据，支持返回/前进与站点式浏览
    > 体验入口： [去首页](#section-home)
  - 星云成就系统：登录后成就进度同步至数据库，智算星云面板展示成就轮播（横线段形式，展示 5 个最接近完成的成就），点击可打开成就统计面板；成就列表含新手指南、公式、脚本、模板、错题本等，进度条与解锁状态持久化
    > 体验入口： [去智算星云](#section-home)
  - 右键菜单区域专属功能：在智能体对话区右键可「重新执行」最后一条消息；在智能识别画板区可快速切换画笔/橡皮擦、撤销/重做、清空画布；在教学案例与动态计算视频区可控制播放/暂停、音量、全屏，无需离开当前区域
    > 体验入口： [去智能体](#section-agent) / [去智能识别](#section-detect) / [去动态计算](#section-calculate) / [去教学案例](#section-examples)
  - 子页面浏览历史：返回/前进按钮及 Alt+←/→ 快捷键在应用内子页面（首页、智能体、智能识别、动态计算、教学案例等）间切换，而非浏览器 URL 历史；浏览器原生前进/后退按钮同步该逻辑，地址栏随子页面更新
    > 体验入口： [去首页](#section-home)
  - 右键菜单页面专属快捷入口：首页显示「新手指南」、我的算式显示「新建算式」、动态计算显示「开始计算」与「选择算式」、教学案例显示「刷新列表」、开发者工具显示 LaTeX/Manim 切换，右键即可直达
    > 体验入口： [去首页](#section-home) / [去动态计算](#section-calculate) / [去教学案例](#section-examples) / [去开发者工具](#section-devtools)
  - AI 编辑助手体验调整：进入 Manim 云端代码工作台时 AI 编辑面板默认展开，便于快速使用自然语言编辑代码；未登录用户点击发送会弹出登录框并提示「请先登录后使用 AI 编辑」，保障功能按账号使用
    > 体验入口： [去开发者工具](#section-devtools)
  - 智能体知识图谱意图：智能体工具调用使用知识图谱判断用户意图，教学案例中的收藏、稍后看、我的课件、全部案例等作为图谱节点，用户说「打开收藏」「稍后看」等可直达对应筛选；图谱节点统一通过 executeNodeAction 执行，每个节点均可正确点击切换到对应功能
    > 体验入口： [去首页](#section-home) / [去智能体](#section-agent)
  - 系统设置接入知识图谱：外观、账户、智能体、画板、快捷键、动态计算、开发者工具、教学案例与弹幕等设置子项作为图谱节点，点击可打开设置并定位到对应区块；智能体可帮用户修改设置（如深色模式、回车发送、默认模式、弹幕开关等），支持 setting_key/setting_value 参数
    > 体验入口： [去首页](#section-home) / [去智能体](#section-agent) / [去设置](#section-settings)
- 优化
  - 按角色快速开始视觉升级：四角色卡片改为「知识图片」式设计，每个角色配专属渐变配色（学生青绿、教师琥珀、创作者紫色、开发者蓝色），顶部视觉区含光晕与图标，hover 有弹性动效，整体更具科技感与交互感
    > 体验入口： [去首页角色入口](#section-home-roles)
  - 弹窗自动关闭与 ESC 支持：切换子页面时自动关闭登录、设置、视频弹窗等所有已打开弹窗；按 ESC 键关闭最顶层弹窗，支持多层弹窗依次关闭
    > 体验入口： [去首页](#section-home)
  - 智算星云面板展开方向：折叠态小球拖到左侧时，展开后面板显示在左侧，拖到右侧则显示在右侧，避免展开后遮挡内容
    > 体验入口： [去智算星云](#section-home)
  - 浮动提示样式与稳定性：浅色/深色模式分别采用更易区分的配色；修复鼠标移出后提示偶发不消失的问题，离开窗口时立即隐藏
    > 体验入口： [全站](#section-home)
  - 右键菜单精简与教学案例视频兼容：将快捷导航、开发者工具、文档帮助合并为单一「更多」子菜单，移除另存为/打印等低频项；教学案例视频弹窗内使用浏览器原生右键菜单，支持保存视频等操作，避免与自定义菜单冲突
    > 体验入口： [去教学案例](#section-examples)
  - 开发者工具默认标签：进入开发者工具时默认打开「云端代码工作台」（Manim 工作台）；设置页「默认打开标签」中该选项文案同步为「云端代码工作台」，恢复默认时亦采用云端代码工作台
    > 体验入口： [去开发者工具](#section-devtools) / [去设置](#section-settings)
  - 智算星云小球水面：水面顶部改为正弦波起伏形状（path 近似 y=sin(x)），波幅适中，波浪水平流动，更像真实波浪起伏
    > 体验入口： [去智算星云](#section-home)
  - 通用模式进度条：通用模式（计算+可视化）使用整体 0–100% 进度体系，计算阶段 0–75%、可视化阶段 75–100%，计算完成后平滑进入后半段，不再出现跳至 99.9% 的割裂感
    > 体验入口： [去动态计算](#section-calculate)
  - 教学案例视频源：默认使用本地视频，保留 CDN 接口；环境变量 EXAMPLES_USE_CDN=1 可启用 CDN 加速
    > 体验入口： [去教学案例](#section-examples)
  - 全站知识图谱卡片与面板：图谱节点卡片采用高级柔和配色，玻璃感、更大圆角，贴合主页 feature-card 风格；图谱面板无边感设计，去除边框与硬阴影，背景与页面自然融合；流程面板与控制按钮同步柔和玻璃态风格
    > 体验入口： [去首页](#section-home)
- 数据
  - 成就表 `user_achievements`：存储用户成就 ID、进度、解锁状态，支持 `/api/achievements/list` 与 `/api/achievements/upsert` 读写，登录后自动同步
    > 体验入口： [去智算星云](#section-home)
  - 算式主题标签表 `formula_topics`、课件包与视频关联等表结构与索引优化，支撑知识星云与掌握度统计
    > 体验入口： [去我的算式](#section-formulas)

## v 0.4.2
#### 智能识别稳定性、整题模式与工作台布局优化
`2026.03.03`
- 新增
  - 智算星云车站系统：知识星云顶部地铁式导航增加「当前站」胶囊提示，明确当前所处位置（如动态计算、开发者工具等），配合横向站点按钮组成「当前位置 + 上下站」视图
    > 体验入口： [去首页](#section-home)
  - 知识图谱节点扩展：将智能识别结果区的「保存并查看」「去计算」「去 LaTeX 编辑器」按钮及「我的算式」中的「编辑算式」「在 LaTeX 编辑器中编辑」「保存修改」等操作纳入知识图谱节点，智能体与图谱点击均可理解这些细粒度功能
    > 体验入口： [去智能识别](#section-detect) / [去我的算式](#section-formulas)
- 优化
  - 云端代码工作台布局：Manim 渲染工作台中代码区与渲染日志区高度不再随日志内容自动变化，仅通过中间拖拽条手动调整，避免报错日志刷屏时将编辑器「挤扁」；AI 编辑助手浮动面板默认停靠在左侧，视频预览浮动面板默认停靠在右侧，更符合视觉习惯
    > 体验入口： [去开发者工具](#section-devtools)
  - 智能识别画板体验：画笔与橡皮擦各自维护独立的粗细值（滑块、Alt+右键拖动与快捷键只影响当前工具），在工具之间切换时自动记住各自的大小；橡皮擦的物理大小在同一数值下显著大于画笔，便于大面积擦除草稿
    > 体验入口： [去智能识别](#section-detect)
  - 教学案例课包入口占位：精选教学案例页中的「创建课包」按钮暂时标记为未实装（按钮禁用并给出「功能开发中」提示），保留入口与文案，避免用户误以为当前即可完整使用课包流水线
    > 体验入口： [去教学案例](#section-examples)
- 修复
  - 智能算式识别 LaTeX 清洗：`/api/detect` 统一对多模态大模型返回的公式使用 `sanitize_latex_for_mathlive` 规范化处理，自动剥离外层 ```latex、`$$ $$`、`\[ \]`、`\(\)` 等包裹，避免「∫_0^1 x^2 dx」一类积分在前端渲染时出现额外上下限或格式异常
    > 体验入口： [去智能识别](#section-detect)
  - 公式识别准确性提示：强化 qwen-vl-max 的图片识别提示词，明确要求逐符号抄写、禁止将 ∫\_0^1 误改为 ∫\_0^∞ 或随意加入系数（如 3x^2），看不清时用问号占位而非猜测，使智能识别在常见教材公式上的鲁棒性显著提升
    > 体验入口： [去智能识别](#section-detect)
  - 智能体整题模式兜底：当用户提供整道题目（含「已知」「下列」「求极限」「泰勒展开」「幂级数」「微分方程」等关键词）或整题截图时，意图识别默认偏向动态计算的整题模式 `operation=solution`；若大模型仍只返回单一公式，后端会自动用**用户原始整题文本**填充公式字段，确保动态计算页拿到的是整道题而不是孤立的一个式子
    > 体验入口： [去智能体](#section-agent) / [去动态计算](#section-calculate)

## v 0.4.3
#### 成就徽章、帮助搜索与移动端联动优化
`2026.03.06`
- 新增
  - 星云 3D 成就徽章：为每个成就提供独立的 3D 徽章弹窗（含立体旋转动画与说明文案），登录用户在首次达成时会自动弹出，并可从成就统计或徽章墙中再次查看
    > 体验入口： [去智算星云](#section-home)
  - 成就统计徽章墙：在成就统计面板顶部新增「徽章墙」区域，以悬挂小徽章的形式展示已解锁成就，点击任意徽章会打开对应的 3D 徽章页面，形成「进度列表 + 荣誉墙」的双视图
    > 体验入口： [去智算星云](#section-home)
  - 帮助页内搜索：在帮助页顶部新增搜索框，支持按关键字实时过滤下方折叠问答，并自动展开命中的问题，便于在手机端快速查找具体使用说明
    > 体验入口： [去帮助](#section-help)
  - 注册密码强度提示：登录 / 注册弹窗的注册表单中新增密码强度进度条与提示文案，仅做可视化提醒（不限制密码策略），引导用户设置更安全的口令
    > 体验入口： [去首页](#section-home)
- 优化
  - 成就系统登录策略：仅对已登录用户计算并持久化成就进度，未登录访问不再请求成就接口，也不会弹出成就相关提示或徽章弹窗，避免游客状态下的误解锁与性能浪费
    > 体验入口： [去智算星云](#section-home)
  - 成就导航与交互：成就统计面板中的列表卡片点击仍按成就类型导航到对应功能区（我的算式、开发者工具、教学案例、帮助等），而徽章墙上的小徽章点击则专注打开 3D 徽章页面，清晰区分「去办事」与「看勋章」
    > 体验入口： [去智算星云](#section-home)
  - 成就徽章动效性能：3D 徽章旋转改为基于 GPU transform 的轻量动画，减少持续 JS 插值计算，在低端设备与移动端上交互更顺滑，拖拽旋转时不会感觉卡顿
    > 体验入口： [去智算星云](#section-home)
- 修复
  - 成就重复解锁提示：成就接口新增 `db_unlocked` 字段，前端只在数据库首次标记为解锁时弹出成就页面与 3D 徽章，刷新页面或再次进入同一页面时不再反复弹出
    > 体验入口： [去智算星云](#section-home)
  - 移动端菜单与星云不同步：修复移动端汉堡菜单仅切换 Section 而不刷新智算星云内容的问题，现在通过统一调用全局 `showSection`，菜单切换页面时星云统计面板会同步切换到当前页面视图
    > 体验入口： [去首页](#section-home)
